---
openapi: get /api/descriptors
---

<Warning>
This is the legacy descriptors endpoint. New integrations should use [Fetch Descriptors (v2)](/api-reference/enrollment/fetch-descriptors-v2), which returns a flat, filterable record set with per-descriptor program, status, and merchant id. Switch to the **v3** version in the sidebar version dropdown.
</Warning>